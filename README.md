# PostScript-Interpreter
Using python to replicate to make a postscript interpreter

TODO: Looks like list inputs are not taken into account, need to deal with this, possibly won't affect functionality, maybe keep it as a string until its needed and tokenize it?
